// in old days Api was used to get data from the server
// now we use it to get data from the local storage, xml, json, etc.

console.log(
  "in json the keys are always in double quotes,but in js they are not required to be in double quotes eventhough they are strings"
);

console.log("api are also in objects under array");

console.log(
  "use json formator website, it is easy to read and write, and it tells you what is wrong in your json, and what data type is expected"
);
